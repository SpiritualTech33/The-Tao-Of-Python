# 2.6 Katas of Chapter 2

> *"From the One come the Many. From the Many, organized, comes One again."*

---

## The 7 Katas

Open `katas/chapter_02_katas.py` and implement each function.

| Kata | Concept |
|------|---------|
| 1. `get_even_numbers` | List comprehension |
| 2. `remove_duplicates` | Sets — preserving order |
| 3. `build_player_profile` | Dictionaries |
| 4. `find_common_elements` | Set intersection |
| 5. `invert_dictionary` | Dict comprehension |
| 6. `sort_players_by_average` | Sorting with key |
| 7. `unpack_coordinates` | Tuple unpacking |

---

## How to Run

```bash
cd path/to/Python-From-The-Void/katas
python -m pytest chapter_02_katas.py -v
```

---

## The Protocol

Implement. Run. Observe. Fix. Repeat. When all 7 pass — move to Chapter 3.

*The many organized become one. That is the kata.*
